        {/* 3D Flip Card — front: image / back: drug details */}
        <div className="flip-outer" style={{ height: '160px', marginBottom: '16px' }}>
          <div className="flip-inner">
            {/* FRONT FACE */}
            <div className="flip-face" style={{ backgroundColor: '#f8fafc', borderRadius: '8px', overflow: 'hidden' }}>
              {hasRealImage ? (
                <img
                  src={imageUrl}
                  alt={product.name}
                  onError={() => setImageError(true)}
                  className="w-full h-full object-contain p-2 mix-blend-multiply"
                  loading="lazy"
                />
              ) : (
                <MedicinePlaceholder
                  name={product.name}
                  category={product.category}
                  is_chronic={product.is_chronic}
                  size="md"
                />
              )}
              {product.is_chronic && (
                <span className="absolute top-2 right-2 bg-primary/90 backdrop-blur-sm text-white px-2 py-0.5 rounded text-[10px] font-bold z-20">
                  Chronic
                </span>
              )}
              {isOutOfStock && (
                <div className="absolute inset-0 bg-white/50 backdrop-blur-[1px] flex items-center justify-center z-30">
                  <span className="bg-error text-white px-3 py-1 rounded-full text-xs font-bold shadow-sm">
                    Out of Stock
                  </span>
                </div>
              )}
            </div>

            {/* BACK FACE — drug quick-info */}
            <div className="flip-face flip-back">
              <p className="text-[10px] font-bold text-primary uppercase tracking-wider">{product.category}</p>
              <p className="text-[12px] font-semibold text-on-surface line-clamp-2 capitalize">{product.name}</p>
              {product.manufacturer && (
                <p className="text-[11px] text-on-surface-variant">by {product.manufacturer}</p>
              )}
              {product.default_cycle_days && (
                <p className="text-[11px] text-on-surface-variant">{product.default_cycle_days}-day refill cycle</p>
              )}
              <p className="text-[11px] text-on-surface-variant mt-auto">
                {product.is_chronic ? '🔁 Chronic medication' : '💊 General indication'}
              </p>
              <p className="text-[12px] font-bold text-primary">EGP {price.toFixed(2)}</p>
            </div>
          </div>
        </div>